
const initialState = {
  items: []
};

const ItemSell = (state = initialState, action) => {
  switch (action.type) {
    
    default:
      return {
        ...state,
      };
  }
};

export default ItemSell;